import { DateTime } from './luxon.js';

const listNav = document.querySelectorAll('.listNav');
const timeZone = document.getElementById('#times-zone');
const switchViews = (sectionDisplayed) => {
    const sectionDisplayed =document.querySelectorAll('.sectionDisplayed');
}